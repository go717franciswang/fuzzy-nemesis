<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to t('title'), stocks_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to t('stock.index'), stocks_path %></li>
          <li><%= link_to t('stock.register'), new_stock_path %></li>
          <li><%= link_to t('stock.simulation_results'), simulations_stocks_path %></li>
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t 'lang.uage' %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
              <li><%= link_to t('lang.en'), url_for({locale: 'en'}) %></li>
              <li><%= link_to t('lang.zh-CN'), url_for({locale: 'zh-CN'}) %></li>
            </ul>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</header>
